// hotel-growth-os/frontend/src/pages/DashboardPage.tsx
import React from 'react';
import DashboardSummary from '../components/DashboardSummary'; 
// We'll create this next for operational data:
import BookingList from '../components/BookingList'; 


interface DashboardProps {
    hotelId: string; // This ID is retrieved after successful login/tenant identification
}

const DashboardPage: React.FC<DashboardProps> = ({ hotelId }) => {
    return (
        // Tailwind CSS classes for styling the layout
        <div className="min-h-screen bg-gray-50 p-6 sm:p-12 font-sans">
            {/* Header Section */}
            <header className="mb-8 border-b pb-4">
                <h1 className="text-3xl font-extrabold text-gray-900">Hotel Growth OS</h1>
                <p className="mt-1 text-lg text-indigo-600">Dashboard for Hotel ID: {hotelId}</p>
            </header>

            {/* ------------------------------------------ */}
            {/* SECTION 1: CORE METRICS (The "OS" Health Score) */}
            <section className="mb-8">
                <h2 className="text-xl font-bold text-gray-700 mb-4">Operational Health Snapshot</h2>
                {/* Our Summary Component goes here, consuming the AnalyticsService data */}
                <DashboardSummary hotelId={hotelId} /> 
            </section>

            {/* ------------------------------------------ */}
            {/* SECTION 2: OPERATIONAL DATA (The PMS View) */}
            <section className="mb-10 bg-white p-6 shadow rounded-lg">
                <h2 className="text-xl font-bold text-gray-700 mb-4 border-b pb-2">Recent Reservations</h2>
                {/* This component will call the /api/v1/bookings endpoint */}
                <BookingList hotelId={hotelId} /> 
            </section>

            {/* ------------------------------------------ */}
            {/* SECTION 3: GROWTH & AUTOMATION (The "Growth" Layer) */}
            <section className="mt-8 bg-white p-6 shadow rounded-lg">
                <h2 className="text-xl font-bold text-gray-700 mb-4 border-b pb-2">Automation Status & Growth Opportunities</h2>
                {/* Future components: Review widget, SEO health score widget, AI Agent status panel */}
                <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div className="p-4 border rounded bg-gray-50 text-center">
                        <h4 className='font-semibold'>Media Buying</h4>
                        <p className='text-sm text-gray-500'>Status: L0 - Observe</p>
                    </div>
                     <div className="p-4 border rounded bg-gray-50 text-center">
                        <h4 className='font-semibold'>SEO/GEO</h4>
                        <p className='text-sm text-gray-500'>Status: L1 - Analyze</p>
                    </div>
                     <div className="p-4 border rounded bg-gray-50 text-center">
                        <h4 className='font-semibold'>Growth Plan</h4>
                        <p className='text-sm text-gray-500'>Next step: Implement Phase 3 (SEO/GEO)</p>
                    </div>
                </div>
            </section>

        </div>
    );
};

export default DashboardPage;
